% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kaggle.R
\name{sd_download_model}
\alias{sd_download_model}
\title{Download a Stable Diffusion model from Kaggle Models}
\usage{
sd_download_model(
  handle = "lbsbmsu/flux-2/gguf/default",
  dest,
  version = NULL,
  files = NULL,
  verbose = FALSE
)
}
\arguments{
\item{handle}{Model handle in kagglehub form
\code{"owner/model/framework/variation"}. Defaults to
\code{"lbsbmsu/flux-2/gguf/default"} -- a ready-to-use FLUX 2 (GGUF) model,
so newcomers can call \code{sd_download_model(dest = "models/flux2")}.}

\item{dest}{Destination directory for the unpacked files. Created if it does
not exist. Required.}

\item{version}{Integer version number. If \code{NULL} (default) the latest
version is resolved automatically from Kaggle.}

\item{files}{Optional character vector of file names to extract from the
bundle. If \code{NULL} (default) all files are extracted.}

\item{verbose}{Logical; print progress messages. Defaults to \code{FALSE}.}
}
\value{
The path to \code{dest} (invisibly), containing the model files.
}
\description{
Downloads a model bundle from the public Kaggle Models registry and unpacks
it into \code{dest}. Mirrors the behaviour of the Python \code{kagglehub}
package (\code{kagglehub.model_download("owner/model/framework/variation")})
but uses only base R -- no Python dependency.
}
\details{
Kaggle serves each model version as a single \code{.tar.gz} bundle; the whole
bundle is downloaded even when only some files are needed. Only public models
are supported.
}
