% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{sd_sample_stepwise}
\alias{sd_sample_stepwise}
\title{Run the sampling loop step-by-step in R (low-level)}
\usage{
sd_sample_stepwise(
  ctx,
  cond,
  uncond = list(crossattn = NULL, vector = NULL, concat = NULL),
  latent_shape = NULL,
  init_latent = NULL,
  noise = NULL,
  width = 512L,
  height = 512L,
  sample_method = SAMPLE_METHOD$EULER,
  scheduler = SCHEDULER$DISCRETE,
  sample_steps = 20L,
  cfg_scale = 7,
  seed = 42L,
  custom_sigmas = NULL,
  on_step = NULL
)
}
\arguments{
\item{ctx}{SD context}

\item{cond}{Positive conditioning from \code{\link{sd_encode_text}}}

\item{uncond}{Negative conditioning; empty (all \code{NULL}) disables CFG}

\item{latent_shape}{Integer \code{c(W, H, C)} in latent space, used to make
noise when \code{noise} is \code{NULL}}

\item{init_latent}{Optional starting latent (img2img); \code{NULL} for txt2img}

\item{noise}{Optional explicit noise sd_tensor; generated from \code{seed}
and \code{latent_shape} when \code{NULL}}

\item{width, height}{Generation size in PIXELS (for the sigma schedule)}

\item{sample_method}{\code{SAMPLE_METHOD$EULER} or \code{$EULER_A}}

\item{scheduler}{Scheduler (name or \code{SCHEDULER} value)}

\item{sample_steps}{Number of steps}

\item{cfg_scale}{CFG scale}

\item{seed}{Seed for noise generation when \code{noise} is \code{NULL}}

\item{custom_sigmas}{Optional explicit sigma schedule (overrides scheduler)}

\item{on_step}{Optional callback \code{function(step, total, x, denoised)}
called after each step; return \code{FALSE} to stop early.}
}
\value{
An sd_tensor — the denoised latent x_0.
}
\description{
Equivalent to \code{\link{sd_sample}} for the Euler / Euler-a samplers, but
runs the loop in R so a callback can observe or interrupt each step (e.g.
live preview). For Euler (no ancestral noise) the result is bit-for-bit equal
to \code{sd_sample}; Euler-a differs (R RNG vs ggml RNG for the ancestral
term). Other samplers are not supported here — use \code{\link{sd_sample}}.
}
\seealso{
\code{\link{sd_sample}}, \code{\link{sd_decode_latent}}
}
