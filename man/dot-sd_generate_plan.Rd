% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{.sd_generate_plan}
\alias{.sd_generate_plan}
\title{Build an executable step plan for sd_generate (async orchestration)}
\usage{
.sd_generate_plan(
  ctx,
  prompt,
  negative_prompt = "",
  width = 512L,
  height = 512L,
  init_image = NULL,
  strength = 0.75,
  sample_method = SAMPLE_METHOD$EULER,
  sample_steps = 20L,
  cfg_scale = 7,
  seed = 42L,
  batch_count = 1L,
  scheduler = SCHEDULER$DISCRETE,
  clip_skip = -1L,
  eta = 0,
  flow_shift = NULL,
  hr_strength = 0.4,
  hr_steps = NULL,
  upscaler = NULL,
  upscale_factor = 4L,
  vae_mode = "auto",
  vae_auto_threshold = 1048576L,
  vae_tile_size = 64L,
  vae_tile_overlap = 0.25,
  cache_mode = c("off", "easy", "ucache"),
  cache_config = NULL
)
}
\arguments{
\item{ctx}{SD context created by \code{\link{sd_ctx}}}

\item{prompt}{Text prompt describing desired image}

\item{negative_prompt}{Negative prompt (default "")}

\item{width}{Image width in pixels (default 512)}

\item{height}{Image height in pixels (default 512)}

\item{init_image}{Optional init image for img2img. If provided, runs img2img
instead of txt2img. Requires \code{vae_decode_only = FALSE}.}

\item{strength}{Denoising strength for img2img (default 0.75). Ignored for
txt2img.}

\item{sample_method}{Sampling method (see \code{SAMPLE_METHOD})}

\item{sample_steps}{Number of sampling steps (default 20)}

\item{cfg_scale}{Classifier-free guidance scale (default 7.0)}

\item{seed}{Random seed (-1 for random)}

\item{batch_count}{Number of images to generate (default 1)}

\item{scheduler}{Scheduler type (see \code{SCHEDULER})}

\item{clip_skip}{Number of CLIP layers to skip (-1 = auto)}

\item{eta}{Eta parameter for DDIM-like samplers}

\item{flow_shift}{Flow shift for flow-matching models (Flux, SD3). \code{NULL}
(default) lets the model pick an architecture-specific value; set a numeric
value to override. Ignored by non-flow models.}

\item{hr_strength}{Denoising strength for highres fix refinement pass
(default 0.4). Only used when auto-routing selects highres fix.}

\item{vae_mode}{VAE processing mode: \code{"normal"}, \code{"tiled"}, or
\code{"auto"} (VRAM-aware: queries free GPU memory and enables tiling
only when estimated peak VAE usage exceeds available VRAM minus a 50 MB
reserve). Default \code{"auto"}.}

\item{vae_tile_size}{Tile size for VAE tiling (default 64)}

\item{vae_tile_overlap}{Overlap for VAE tiling (default 0.25)}

\item{cache_mode}{Step caching mode: \code{"off"} (default), \code{"easy"}
(EasyCache), or \code{"ucache"} (UCache).}

\item{cache_config}{Optional fine-tuned cache config from
\code{\link{sd_cache_params}}.}
}
\value{
List of step descriptors (see above).
}
\description{
Mirrors the routing logic of \code{\link{sd_generate}} (cfg auto-1.0 for
guidance-distilled Flux/Flux.2, strategy selection, VAE tiling resolution)
but instead of running the pipeline, returns a list of \emph{steps}. Each
step is one of:
\itemize{
  \item \code{type = "gen"}  — a single \code{sd_generate_async()} call.
    Carries a ready-to-use \code{params} list, \code{width}/\code{height},
    a \code{label}, and \code{uses_init} (whether it consumes the previous
    step's image as \code{init_image}).
  \item \code{type = "upscale"} — a synchronous R-side resize/ESRGAN step
    run between two gen steps. Carries \code{width}/\code{height},
    \code{upscaler}, \code{upscale_factor}.
}
The final step (the image returned to the caller) has \code{final = TRUE}.
}
\details{
This lets the Shiny GUI drive the multi-step highres-fix pipeline through the
single-shot async engine: run a gen step, poll it, feed its result into the
next step, all without blocking the R session.
}
\keyword{internal}
