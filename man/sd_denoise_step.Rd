% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{sd_denoise_step}
\alias{sd_denoise_step}
\title{Run a single denoise step (low-level)}
\usage{
sd_denoise_step(
  ctx,
  x,
  sigma,
  cond,
  uncond = list(crossattn = NULL, vector = NULL, concat = NULL),
  cfg_scale = 7,
  step = 1L,
  total_steps = 1L
)
}
\arguments{
\item{ctx}{SD context}

\item{x}{Current latent sd_tensor}

\item{sigma}{Current sigma (scalar)}

\item{cond}{Positive conditioning from \code{\link{sd_encode_text}}}

\item{uncond}{Negative conditioning; empty (all \code{NULL}) disables CFG}

\item{cfg_scale}{CFG scale (1 disables CFG)}

\item{step, total_steps}{1-based step index / total, for progress hooks}
}
\value{
An sd_tensor list — the denoised x_0 estimate.
}
\description{
Runs the diffusion model once on \code{x} at \code{sigma} and returns the
denoised x_0 estimate. The Euler update of \code{x} is done by the caller
(see \code{\link{sd_sample_stepwise}} for the full loop). Must be called
between \code{\link{sd_sampler_begin}} and \code{\link{sd_sampler_end}}.
}
\seealso{
\code{\link{sd_sample_stepwise}}
}
