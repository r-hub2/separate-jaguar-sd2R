% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{sd_encode_text}
\alias{sd_encode_text}
\title{Encode a text prompt into conditioning (low-level)}
\usage{
sd_encode_text(ctx, prompt, clip_skip = -1L, width = -1L, height = -1L)
}
\arguments{
\item{ctx}{SD context from \code{\link{sd_ctx}}}

\item{prompt}{Text prompt}

\item{clip_skip}{CLIP layers to skip (-1 = model default)}

\item{width, height}{Intended generation size (affects size-conditioning for
some models, e.g. SDXL). -1 lets the model decide.}
}
\value{
A conditioning list with elements \code{crossattn}, \code{vector},
  \code{concat}; each is an sd_tensor list (\code{type}, \code{ne},
  \code{data}) or \code{NULL} when the model does not produce it.
}
\description{
Runs only the text-encoder stage of the pipeline, returning the
conditioning tensors (analogue of \code{SDCondition}). Building block for
custom pipelines; most users want \code{\link{sd_generate}}.
}
\seealso{
\code{\link{sd_sample}}, \code{\link{sd_decode_latent}}
}
