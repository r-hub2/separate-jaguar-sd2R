% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{sd_generate_multiref}
\alias{sd_generate_multiref}
\title{Generate an image conditioned on multiple reference images}
\usage{
sd_generate_multiref(
  ctx,
  prompt,
  refs,
  negative_prompt = "",
  width = 512L,
  height = 512L,
  auto_resize_ref_image = TRUE,
  increase_ref_index = FALSE,
  sample_method = SAMPLE_METHOD$EULER,
  scheduler = SCHEDULER$DISCRETE,
  sample_steps = 20L,
  cfg_scale = 7,
  seed = 42L,
  clip_skip = -1L,
  eta = 0,
  batch_count = 1L
)
}
\arguments{
\item{ctx}{SD context from \code{\link{sd_ctx}}}

\item{prompt}{Text prompt}

\item{refs}{A list of sd_image lists (each with \code{width}, \code{height},
\code{channel}, \code{data}), e.g. from \code{\link{sd_load_image}}.}

\item{negative_prompt}{Negative prompt (default "")}

\item{width, height}{Output size in pixels}

\item{auto_resize_ref_image}{If \code{TRUE} (default), references are resized
to fit the model's expected reference size.}

\item{increase_ref_index}{If \code{TRUE}, reference latents get increasing
positional indices (model-specific; default \code{FALSE}).}

\item{sample_method, scheduler}{Sampler / scheduler (name or enum value)}

\item{sample_steps, cfg_scale, seed, clip_skip, eta}{Standard sampling controls}

\item{batch_count}{Number of images (default 1)}
}
\value{
List of sd_image lists.
}
\description{
Runs generation with one or more reference images, as used by edit /
reference-conditioned models (e.g. Qwen-Image, FLUX control/edit variants).
The references are passed straight through to the underlying
\code{generate_image} C-API (\code{ref_images}); the active model decides how
to use them, so this only has effect on models that support reference
conditioning.
}
\seealso{
\code{\link{sd_generate}}, \code{\link{sd_encode_image}}
}
